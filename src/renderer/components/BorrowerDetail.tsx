import React from "react"
import { fetch } from '../utils/dbaccess'

export const BorrowerDetail = () => {
    
    console.log(fetch(1, 'id'));
    
    return (
        <div>
            
        </div>
    )
}